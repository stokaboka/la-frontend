export const lang = s => {
  return s.lang
}

export const langs = s => {
  return s.langs
}

export const strings = s => {
  return s.strings[s.lang]
}

export const about = s => {
  return s.strings[s.lang].about
}

export const header = s => {
  return s.strings[s.lang].header
}

export const info = s => {
  return s.strings[s.lang].info
}

export const auth = s => {
  return s.strings[s.lang].auth
}

export const error = s => {
  return s.strings[s.lang].error
}

export const home = s => {
  return s.strings[s.lang].home
}

export const partone = s => {
  return s.strings[s.lang].partone
}

export const laonecontainer = s => {
  return s.strings[s.lang].laonecontainer
}

export const laone = s => {
  return s.strings[s.lang].laone
}

export const latwo = s => {
  return s.strings[s.lang].latwo
}

export const latree = s => {
  return s.strings[s.lang].latree
}

export const cloze = s => {
  return s.strings[s.lang].cloze
}

export const groupMultiChoice = s => {
  return s.strings[s.lang].groupMultiChoice
}

export const multiChoice = s => {
  return s.strings[s.lang].multiChoice
}
